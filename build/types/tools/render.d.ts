import { MinTreeNode } from "../nodes/mintree_node";
/**
 *  Takes a mintree node and produces a string comprising the rendered productions of the ast.
 */
export declare function render(node: MinTreeNode): string;
